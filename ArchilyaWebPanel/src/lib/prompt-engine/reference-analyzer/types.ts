export type { GeometryRisk, ReferenceBrief, ReferencePolicy, ReferenceType } from "../types";

export interface ReferenceAnalyzerInput {
  type?: string;
  label?: string;
  note?: string;
  toolId?: string;
}
